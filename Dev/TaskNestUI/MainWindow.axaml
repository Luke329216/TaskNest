<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        x:Class="TaskNestUI.MainWindow"
        Width="1300"
        Height="860"
        Title="TaskNest"
        Background="{DynamicResource WindowBackground}">


    <Grid RowDefinitions="Auto,*" Margin="20">
        <StackPanel Orientation="Vertical" Spacing="8">
            <TextBlock Text="TaskNest" FontSize="34" FontWeight="Bold" Foreground="{DynamicResource PrimaryText}"/>
            <TextBlock Text="A modern productivity workspace for tasks and categories." FontSize="15" Foreground="{DynamicResource SubtleText}"/>
        </StackPanel>

        <TabControl Name="MainTabs" Grid.Row="1" TabStripPlacement="Top" Margin="0,20,0,0">
            <TabItem>
                <TabItem.Header>
                    <TextBlock Text="Tasks" Foreground="{DynamicResource PrimaryText}" FontWeight="SemiBold"/>
                </TabItem.Header>
                <Grid ColumnDefinitions="2*,1*" Margin="0,8,0,0">
                    <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto" Margin="0,0,20,0">
                        <StackPanel Spacing="24">
                            <Border Classes="card">
                                <StackPanel Spacing="18">
                                    <Grid ColumnDefinitions="*,Auto">
                                        <StackPanel Spacing="8">
                                            <TextBlock Text="TaskNest dashboard" FontSize="28" FontWeight="Bold" Foreground="{DynamicResource PrimaryText}"/>
                                            <TextBlock Text="Everything you need to create tasks, organize categories, and move faster." Foreground="{DynamicResource SubtleText}" FontSize="14"/>
                                        </StackPanel>
                                        <Border Background="{DynamicResource AccentColor}" CornerRadius="12" Padding="10,6" Grid.Column="1" VerticalAlignment="Top">
                                            <TextBlock Text="Professional" Foreground="{DynamicResource PrimaryText}" FontWeight="SemiBold"/>
                                        </Border>
                                    </Grid>

                                    <Grid ColumnDefinitions="*,Auto" ColumnSpacing="12" VerticalAlignment="Center">
                                        <TextBox Name="TaskInput" PlaceholderText="Add a new task…" />
                                        <Button Grid.Column="1" Content="Add Task" Click="AddTaskToGeneral_Click" MinWidth="140"/>
                                    </Grid>

                                    <Button Content="Undo Last Delete" Click="Undo_Click" HorizontalAlignment="Left" Width="180" Background="{DynamicResource MutedButtonBackground}"/>
                                </StackPanel>
                            </Border>

                            <Border Classes="card">
                                <StackPanel Spacing="16">
                                    <TextBlock Text="Task categories" FontSize="22" FontWeight="Bold" Foreground="{DynamicResource PrimaryText}"/>
                                    <TextBlock Text="Use the panel to the right to add, rename, and manage categories with a clean layout." Foreground="{DynamicResource SubtleText}" FontSize="14"/>
                                    <Border Background="{DynamicResource SectionBackground}" CornerRadius="16" Padding="18" MinHeight="320">
                                        <StackPanel Spacing="20">
                                            <StackPanel Orientation="Horizontal" Spacing="12">
                                                <TextBlock Text="Categories" FontSize="18" FontWeight="SemiBold" Foreground="{DynamicResource PrimaryText}"/>
                                                <TextBlock Text="Right-click a category for quick actions." Foreground="{DynamicResource SubtleText}" VerticalAlignment="Center"/>
                                            </StackPanel>
                                            <Border Name="CategoryContainer" Background="{DynamicResource SectionBackground}" CornerRadius="14" Padding="16">
                                                <StackPanel x:Name="CategoryPanel" Spacing="16"/>
                                            </Border>
                                        </StackPanel>
                                    </Border>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </ScrollViewer>

                    <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto">
                        <Border Classes="card" MinHeight="520">
                            <StackPanel Spacing="18">
                                <TextBlock Text="Focus panel" FontSize="22" FontWeight="Bold" Foreground="{DynamicResource PrimaryText}"/>

                                <Border Background="{DynamicResource SectionBackground}" CornerRadius="14" Padding="16">
                                    <StackPanel Spacing="8">
                                        <TextBlock Text="General category is your default workspace." Foreground="{DynamicResource SubtleText}" FontSize="13"/>
                                        <TextBlock Text="Hover over a task to see action menus or right-click a category to add tasks quickly." Foreground="{DynamicResource SubtleText}" FontSize="13"/>
                                    </StackPanel>
                                </Border>

                                <Border Background="{DynamicResource SectionBackground}" CornerRadius="14" Padding="16">
                                    <StackPanel Spacing="8">
                                        <TextBlock Text="Pro tips" FontSize="16" FontWeight="Bold" Foreground="{DynamicResource PrimaryText}"/>
                                        <TextBlock Text="• Use categories to separate work from personal tasks." Foreground="{DynamicResource SubtleText}" FontSize="13"/>
                                        <TextBlock Text="• Keep overdue tasks visible and resolve them first." Foreground="{DynamicResource SubtleText}" FontSize="13"/>
                                        <TextBlock Text="• Use priority flags for urgent items." Foreground="{DynamicResource SubtleText}" FontSize="13"/>
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </Border>
                    </ScrollViewer>
                </Grid>
            </TabItem>

            <TabItem>
                <TabItem.Header>
                    <TextBlock Text="Guide" Foreground="{DynamicResource PrimaryText}" FontWeight="SemiBold"/>
                </TabItem.Header>
                <Grid Background="{DynamicResource WindowBackground}" Margin="0,8,0,0">
                    <ScrollViewer>
                        <StackPanel Margin="50" Spacing="24">
                                <StackPanel Spacing="8">
                                <TextBlock Text="Welcome to TaskNest" FontSize="34" FontWeight="Bold" Foreground="{DynamicResource PrimaryText}"/>
                                <TextBlock Text="A polished task workspace designed for clarity and speed." FontSize="16" Foreground="{DynamicResource SubtleText}"/>
                            </StackPanel>

                            <StackPanel Orientation="Vertical" Spacing="24">
                                <Border Classes="card">
                                    <StackPanel Spacing="12">
                                        <TextBlock Text="Getting Started" FontSize="22" FontWeight="Bold" Foreground="{DynamicResource PrimaryText}"/>
                                        <TextBlock Text="1. Create categories." Foreground="{DynamicResource PrimaryText}"/>
                                        <TextBlock Text="2. Add tasks." Foreground="{DynamicResource PrimaryText}"/>
                                        <TextBlock Text="3. Organize work with priority and due dates." Foreground="{DynamicResource PrimaryText}"/>
                                        <TextBlock Text="4. Track progress and stay focused." Foreground="{DynamicResource PrimaryText}"/>
                                    </StackPanel>
                                </Border>
                                <Border Classes="card">
                                    <StackPanel Spacing="12">
                                        <TextBlock Text="Productivity Tips" FontSize="22" FontWeight="Bold" Foreground="{DynamicResource PrimaryText}"/>
                                        <TextBlock Text="• Break large tasks into smaller steps." Foreground="{DynamicResource PrimaryText}"/>
                                        <TextBlock Text="• Keep your active list concise." Foreground="{DynamicResource PrimaryText}"/>
                                        <TextBlock Text="• Use categories to separate contexts." Foreground="{DynamicResource PrimaryText}"/>
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </TabItem>

            <TabItem>
                <TabItem.Header>
                    <TextBlock Text="Settings" Foreground="{DynamicResource PrimaryText}" FontWeight="SemiBold"/>
                </TabItem.Header>
                <Grid Background="{DynamicResource WindowBackground}" Margin="0,8,0,0">
                    <ScrollViewer>
                        <StackPanel Margin="50" Spacing="24">
                            <Border Classes="card">
                                <StackPanel Spacing="16">
                                    <TextBlock Text="Settings" FontSize="28" FontWeight="Bold" Foreground="{DynamicResource PrimaryText}"/>
                                    <TextBlock Text="Choose a workspace style and review app details." Foreground="{DynamicResource SubtleText}"/>
                                    <Border Background="{DynamicResource SectionBackground}" CornerRadius="16" Padding="20">
                                        <StackPanel Spacing="14">
                                            <TextBlock Text="Theme" FontSize="20" FontWeight="SemiBold" Foreground="{DynamicResource PrimaryText}"/>
                                            <TextBlock Text="Select the color style for the app." Foreground="{DynamicResource SubtleText}"/>
                                            <ComboBox Name="ThemePicker" SelectedIndex="0">
                                                <ComboBoxItem Content="🌑 Midnight"/>
                                                <ComboBoxItem Content="🌊 Ocean Blue"/>
                                                <ComboBoxItem Content="🌌 Purple Night"/>
                                                <ComboBoxItem Content="🌲 Emerald"/>
                                                <ComboBoxItem Content="☀ Light Mode"/>
                                            </ComboBox>
                                        </StackPanel>
                                    </Border>
                                </StackPanel>
                            </Border>
                            <Border Classes="card">
                                <StackPanel Spacing="12">
                                    <TextBlock Text="About TaskNest" FontSize="22" FontWeight="Bold" Foreground="{DynamicResource PrimaryText}"/>
                                    <TextBlock Text="Version 2.0" Foreground="{DynamicResource PrimaryText}"/>
                                    <TextBlock Text="Built with Avalonia UI" Foreground="{DynamicResource SubtleText}"/>
                                    <TextBlock Text="TaskNest helps you organize tasks, categories and productivity in one place." Foreground="{DynamicResource SubtleText}" TextWrapping="Wrap"/>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</Window>